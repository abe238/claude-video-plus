# Frozen-Control snapshot-contract proof

`run.json` records a successful network-free execution of detached Control
`83da59fa78c3eee9e20f515fe75c438bb5166efd` under the final P01 contract. It includes the
hash of the generated yt-dlp pass-through wrapper and proves the local no-caption expectation:
expected hash, consumed snapshot path, and consumed hash are all null. Fixture media remains
under `/private/tmp`; no media or extracted frame is committed.
