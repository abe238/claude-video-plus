# Final network-free frozen-Control proof

`run.json` is a successful (`exit_code: 0`) execution receipt for the unchanged detached
Control commit `83da59fa78c3eee9e20f515fe75c438bb5166efd`. It records the local fixture
identity checksum, executable version/content checksums, isolated invocation policy, raw
stdout/stderr references, and post-run clean assertion.

The one-second generated MP4 fixture and empty output directory remain in
`/private/tmp/p01-live-control.T74khW/`; no media or extracted output is committed. This
proof uses `--detail transcript`, `--no-whisper`, offline local source mode, no cookie input,
and real pinned Python/FFmpeg/ffprobe/yt-dlp executables. It does not claim to modify upstream
yt-dlp retry or cookie defaults.
