# P00 exit mapping

## Acceptance evidence

| Requirement | Reviewed evidence |
| --- | --- |
| GOV-001 | Owner-approved master plan, exact-tree Sol gate, frozen identifiers, and `--ready` validation. |
| GOV-002 | Atomic catalog plus registry maps all 45 requirements to 35 issue-backed packets. |
| GOV-003 | Git-object, append-only review, immutable verify/EXIT, local Sol-session, and finalizer-path checks. |

The deterministic finalizer may fill only the identifiers and outcome fields declared in
`PROTOCOL.md`. Any change to this mapping requires a new exact-tree Sol review.
